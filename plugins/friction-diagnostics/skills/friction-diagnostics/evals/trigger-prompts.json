{
  "skill_name": "friction-diagnostics",
  "comment": "Boundary probes for the categorical trigger. This is a test artifact: it probes the edge of the intent, it does not define or enumerate the trigger. The TDD-red negative (negative-01) is the acceptance gate.",
  "queries": [
    {
      "id": "trigger-01",
      "should_trigger": true,
      "query": "The build passed locally but CI failed because the linter config silently ignored the whole directory — that genuinely surprised me. Log this friction."
    },
    {
      "id": "trigger-02",
      "should_trigger": true,
      "query": "While drafting chapter 3 I discovered the outline contradicts the character sheet I was writing from. Record the friction."
    },
    {
      "id": "trigger-03",
      "should_trigger": true,
      "query": "Keep a durable log of any genuine surprises — tools, docs, or instructions behaving differently than predicted — while you work through this."
    },
    {
      "id": "trigger-04",
      "should_trigger": true,
      "query": "That same zsh read-only variable trap just bit again — file it against the earlier event."
    },
    {
      "id": "negative-01",
      "should_trigger": false,
      "query": "Run the test suite now; the new tests should fail because we haven't implemented the feature yet."
    },
    {
      "id": "negative-02",
      "should_trigger": false,
      "query": "The command failed — just fix it and move on."
    },
    {
      "id": "negative-03",
      "should_trigger": false,
      "query": "Summarize why the tests failed."
    },
    {
      "id": "negative-04",
      "should_trigger": false,
      "query": "Start on the task and report your status as you go."
    }
  ]
}
