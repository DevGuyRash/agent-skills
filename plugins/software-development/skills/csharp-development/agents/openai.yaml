interface:
  display_name: "C# Development"
  short_description: "Repository-aware C# and .NET development"
  default_prompt: "Use $csharp-development to implement this C# change against the repository's SDK, target-framework, and API contracts."
policy:
  allow_implicit_invocation: true
