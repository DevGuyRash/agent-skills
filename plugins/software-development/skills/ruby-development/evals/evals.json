{
  "skill_name": "ruby-development",
  "evals": [
    {
      "id": "eval-hash-key-contract",
      "prompt": "Repair a mutable Ruby value object used as a Hash and Set key. Distinct objects with the same logical identity must deduplicate and remain safely lookupable.",
      "expected_output": "Align eql? and hash over the same stable identity fields, preserve the repository's equality and subclass policy, prevent active-key mutation from invalidating indexes, and test lookup and deduplication with distinct equivalent objects.",
      "files": []
    },
    {
      "id": "eval-keyword-api",
      "prompt": "Refactor a public delegator whose callers mix keyword arguments and blocks, and whose published RBS file mirrors the API.",
      "expected_output": "Preserve Ruby-version-appropriate positional, keyword, and block behavior plus any contractual Method#parameters or visibility surface, align RBS with implementation, and test bound and unbound APIs.",
      "files": []
    },
    {
      "id": "eval-queue-worker-lifetime",
      "prompt": "Fix a bounded Queue-based Ruby worker pool that can deadlock when one consumer fails and can return while worker threads remain alive.",
      "expected_output": "Define queue closure and drain semantics, make producer and thread ownership explicit, unblock producers, prevent queued operations from starting after the primary failure unless execution-drain is contractual, settle every worker, preserve exact primary failure identity, and test exceptional shutdown with slow pending work.",
      "files": [
        "evals/files/worker.rb"
      ]
    },
    {
      "id": "eval-subprocess-settlement",
      "prompt": "Repair an Open3-based command runner that deadlocks on large stderr, times out without stopping descendants, and sometimes returns before the child is reaped.",
      "expected_output": "Use direct argument execution, progress both output pipes, distinguish exactly-at-limit EOF from actual truncation, make process-group ownership platform-aware, escalate cancellation by policy even when a descendant closes inherited pipes, observe the owned group boundary and exit status, and reap before returning.",
      "files": []
    }
  ]
}
