{
  "skill_name": "python-development",
  "evals": [
    {
      "id": "eval-dependency-authority-and-import-origin",
      "prompt": "Repair a Python package whose test dependency was moved from its repository-owned dependency group into a similarly named published extra, and whose current smoke test accidentally imports an older globally installed copy.",
      "expected_output": "Restore the repository's established dependency authority without cosmetic tool migration, preserve publication metadata, and verify both the selected interpreter and the imported module origin before treating tests as evidence.",
      "files": []
    },
    {
      "id": "eval-decorated-public-api",
      "prompt": "Repair a decorator applied to async public methods: it breaks descriptor binding, hides the inspectable keyword-only signature used by dependency injection, and reports coroutine functions as ordinary callables. Keep the repository's .pyi contract and configured Pyright checks aligned.",
      "expected_output": "Preserve descriptor and async call shape, keyword names, metadata and inspectable signature, align the stub and configured checker, and add runtime evidence for binding and introspection rather than assuming *args/**kwargs preserves the API.",
      "files": []
    },
    {
      "id": "eval-async-bounded-lifecycle",
      "prompt": "Fix an asyncio dispatcher with a bounded work queue: a worker failure strands queue.join(), cancellation is swallowed during async client cleanup, and producer completion cannot reliably stop every consumer when the queue is full.",
      "expected_output": "Model the queue and workers as one owned protocol, keep the true in-flight bound, make producer and consumer termination deliverable under failure, balance task accounting, settle every task and cleanup operation, preserve useful failures, and propagate cancellation.",
      "files": [
        "evals/files/async_worker.py"
      ]
    },
    {
      "id": "eval-subprocess-terminal-settlement",
      "prompt": "Fix a maintained Python CLI that calls wait() before draining two captured pipes, returns immediately after terminate() on timeout, and can leave a grandchild holding the pipes open. Output may exceed pipe capacity.",
      "expected_output": "Treat both pipes and the process tree as one lifecycle, avoid cyclic waits, define platform-appropriate termination authority, do not infer group settlement from leader exit or pipe EOF, escalate after the grace interval while authority is valid, drain and reap to terminal state, preserve the CLI's output and exit contract, and verify success, failure, high-output, early-pipe-close descendant, and timeout paths.",
      "files": []
    }
  ]
}
