interface:
  display_name: "Kotlin Development"
  short_description: "Repository-aware Kotlin and interop development"
  default_prompt: "Use $kotlin-development to implement this Kotlin change against the repository's compiler, target, and API contracts."
policy:
  allow_implicit_invocation: true
